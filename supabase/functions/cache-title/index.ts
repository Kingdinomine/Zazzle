// Migrated to JavaScript entrypoint to match project preference and avoid IDE TypeScript/Deno errors.
// See neighboring file: ./index.js
// This thin TS module simply delegates to the JS implementation so that local IDEs without Deno tooling don't error.
// No Deno-specific imports here to keep TypeScript happy.

import "./index.js";

export {};
